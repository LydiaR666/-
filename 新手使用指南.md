# 🎯 巨潮资讯并购公告爬虫 - 新手完全使用指南

## 📋 目录
1. [准备工作](#准备工作)
2. [安装步骤](#安装步骤)
3. [使用方法](#使用方法)
4. [查看结果](#查看结果)
5. [常见问题](#常见问题)

---

## 🔧 准备工作

### 你需要准备的文件
你已经有一个Excel文件：`C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx`

这个文件必须包含以下列（表头）：
- ✅ **Symbol** - 股票代码（比如：1, 2, 6, 12 等）
- ✅ **FirstDeclareDate** - 首次公告日期（比如：2007/11/22）
- ✅ **event_id** - 事件ID（比如：1, 2, 3 等）

可选列：
- LatestDeclareDate - 最新公告日期
- FinishDeclareDate - 完成公告日期

> **重要提示**：你的Excel文件表头必须是英文的！如果是中文，需要先改成上面的英文名称。

---

## 💻 安装步骤

### 第一步：检查Python版本

1. 按 `Win + R` 键，输入 `cmd`，按回车打开命令提示符
2. 在黑色窗口中输入以下命令，按回车：

```bash
python --version
```

**期望结果**：应该显示 `Python 3.7.x` 或更高版本

> 如果显示"不是内部或外部命令"，说明你没安装Python，需要先去 https://www.python.org/downloads/ 下载安装。

---

### 第二步：安装必需的Python库

在命令提示符（黑色窗口）中，输入以下命令：

```bash
pip install requests pandas openpyxl beautifulsoup4 lxml PyPDF2 python-dateutil tqdm retry
```

这个命令会自动下载并安装所有需要的工具包，**等待几分钟**直到完成。

**成功标志**：看到 "Successfully installed..." 字样

---

### 第三步：下载爬虫程序

你有两个选择：

#### 方式A：如果你会用Git
```bash
git clone <这个项目的地址>
cd <项目目录>
```

#### 方式B：直接下载（推荐新手）
1. 下载项目的所有文件到一个文件夹，比如 `D:\cninfo_crawler\`
2. 确保文件夹里有这些文件：
   - main.py
   - config.py
   - cninfo_api.py
   - download_manager.py
   - content_validator.py
   - requirements.txt

---

## 🚀 使用方法

### 第一步：打开命令提示符并进入项目目录

1. 按 `Win + R`，输入 `cmd`，按回车
2. 输入以下命令进入项目目录（假设你的文件在 D:\cninfo_crawler\）：

```bash
cd D:\cninfo_crawler
```

或者**更简单的方法**：
- 在文件管理器中打开项目文件夹 `D:\cninfo_crawler\`
- 在地址栏输入 `cmd` 后按回车（会自动在当前目录打开命令提示符）

---

### 第二步：运行爬虫

#### 🎯 方案1：基础用法（推荐）

```bash
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\下载结果"
```

**这条命令的意思**：
- `--input` 后面是你的Excel文件路径（就是你提供的那个文件）
- `--output` 后面是下载结果保存的位置（会自动创建文件夹）

---

#### 🎯 方案2：先测试几条数据（强烈推荐新手）

为了确保程序能正常运行，先测试前2条数据：

```bash
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\测试结果" --sample 2
```

**测试成功后**，再运行完整的数据：

```bash
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\下载结果"
```

---

#### 🎯 方案3：指定日期范围

如果你只想下载某个时间段的公告：

```bash
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\下载结果" --start-date 2020-01-01 --end-date 2023-12-31
```

这样只会处理2020年到2023年的数据。

---

#### 🎯 方案4：加快下载速度

如果你的网络很好，可以增加并发数（同时下载多个文件）：

```bash
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\下载结果" --workers 5
```

> ⚠️ **注意**：不要设置太高（不超过5），否则可能被网站封禁IP。

---

### 运行时你会看到什么？

```
==============================================================
巨潮资讯并购公告爬虫
==============================================================
输入文件: C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx
输出目录: C:\Users\10657\Desktop\并购公告\下载结果
并发数: 3
验证模式: 下载并验证
==============================================================
成功加载并购数据: 9 条记录
字段: Symbol, event_id, FirstDeclareDate, LatestDeclareDate, FinishDeclareDate

开始处理并购事件...
[进度条显示] ████████████████████ 100%

✓ 处理完成!
```

---

## 📊 查看结果

程序运行完成后，在输出目录会生成以下文件：

### 1. 📁 `announcements` 文件夹
包含所有下载的PDF公告文件，文件名格式：
```
事件ID_股票代码_公告日期_公告编号_公司名称_公告标题.pdf
```

例如：
```
1_000001_20071122_1234567_平安银行_关于资产收购的公告.pdf
```

---

### 2. 📄 `download_log.xlsx` - 完整下载记录

包含所有公告的详细信息：
- 事件ID
- 股票代码
- 公告日期
- 公告标题
- 文件名
- 下载状态（成功/失败）
- 验证结果（有效/无效）
- 匹配的关键词类别

---

### 3. ✅ `valid_announcements.xlsx` - 有效公告列表

**这是最重要的文件！** 包含所有通过验证的公告，即包含以下内容的公告：
1. ✅ 企业动机（并购动机、战略目的等）
2. ✅ 标的资产和业务范围（标的公司、业务情况等）
3. ✅ 并购目的（交易目的、收购目的等）
4. ✅ 对收购方影响（对公司影响、交易影响等）

程序要求**至少匹配3类**才算有效公告。

---

### 4. ❌ `invalid_announcements.xlsx` - 无效公告列表

包含未通过验证的公告（比如只是停牌公告、更正公告等，不包含实质内容）。

---

## 🎯 程序的智能功能

### 1. ✅ 完整性保障 - 不遗漏公告

程序会：
- 在**首次公告日前后各扩展7天**搜索
- 搜索到**最新公告日期**和**完成公告日期**
- 使用并购关键词自动过滤相关公告

**示例**：
如果首次公告日是 `2007/11/22`，程序会搜索 `2007/11/15` 到 `2007/11/29` 之间的所有相关公告。

---

### 2. ✅ 智能过滤 - 避免无效公告

自动排除：
- 纯粹的停牌/复牌公告
- 澄清公告
- 问询函
- 不包含实质内容的公告

保留：
- 包含并购实质内容的公告
- 包含4类关键信息的公告

---

### 3. ✅ 内容验证 - 确保公告质量

每个PDF都会被自动检查是否包含：

| 类别 | 关键词示例 |
|------|-----------|
| 企业动机 | 并购动机、战略目的、发展战略、交易背景 |
| 标的资产 | 标的资产、标的公司、业务范围、主营业务 |
| 并购目的 | 交易目的、收购目的、本次交易旨在 |
| 对收购方影响 | 对公司影响、财务影响、经营影响 |

**验证标准**：4类中至少匹配3类 → 有效公告

---

### 4. ✅ 断点续传 - 可随时中断

如果下载过程中：
- 网络中断
- 电脑关机
- 手动停止（按 Ctrl+C）

再次运行同样的命令，程序会：
- 自动跳过已下载的文件
- 继续下载未完成的部分
- 不会重复下载

---

## ❓ 常见问题

### Q1: 程序运行时出现 "FileNotFoundError"

**原因**：Excel文件路径不正确

**解决方法**：
1. 检查路径是否正确（注意使用双引号包裹路径）
2. 确保路径中没有特殊字符
3. 可以把Excel文件复制到项目目录，然后使用简单的文件名：

```bash
python main.py --input 并购公告下载.xlsx --output ./结果
```

---

### Q2: 提示 "缺少必需字段: Symbol"

**原因**：Excel文件的表头不是英文

**解决方法**：
打开Excel文件，将表头改为：
- 股票代码 → `Symbol`
- 首次披露日期 → `FirstDeclareDate`
- 事件ID → `event_id` 或 `EventID`

---

### Q3: 下载速度很慢

**原因**：网络较慢或并发数太低

**解决方法**：
1. 增加并发数（但不要超过5）：
```bash
python main.py --input "你的文件.xlsx" --output "./结果" --workers 5
```

2. 确保网络稳定

---

### Q4: 很多公告被标记为"无效"

**可能原因**：
1. 验证标准太严格
2. PDF文本提取失败

**解决方法**：

打开 `config.py` 文件，找到第93行：

```python
VALIDATION_THRESHOLD = 3  # 改为 2 会降低标准
```

将 `3` 改为 `2`，表示4类中至少匹配2类就算有效。

---

### Q5: 某些公告下载失败

**原因**：
- 网络不稳定
- 公告已被删除
- 巨潮资讯网站的反爬虫机制

**解决方法**：
1. 程序会自动重试3次
2. 如果仍然失败，可以增大请求延迟

打开 `config.py` 文件，找到第18行：

```python
'request_delay': 1,  # 改为 2 或 3
```

---

### Q6: 日期格式问题

**你的Excel中的日期格式**：`2007/11/22`（斜杠分隔）

**程序支持的格式**：
- `2007-11-22`（推荐）
- `2007/11/22`（支持）
- `20071122`（支持）

如果出现日期格式错误，建议在Excel中将日期列改为 `2007-11-22` 格式。

---

### Q7: 如何停止程序？

按 `Ctrl + C` 组合键，程序会安全退出并保存已下载的数据。

---

### Q8: Excel文件有多个Sheet怎么办？

程序默认读取第一个Sheet。如果数据在其他Sheet：
1. 在Excel中将需要的Sheet移到最前面
2. 或者将数据复制到一个新的Excel文件

---

## 💡 使用建议

### 1. 首次使用
```bash
# 先测试2条数据
python main.py --input "你的文件.xlsx" --output "./测试" --sample 2

# 确认没问题后，再运行完整数据
python main.py --input "你的文件.xlsx" --output "./完整结果"
```

---

### 2. 分批处理大量数据

如果数据量很大（比如上千条），建议分批处理：

**第一批：前100条**
```bash
python main.py --input "你的文件.xlsx" --output "./结果_批次1" --sample 100
```

**第二批：101-200条**
先在Excel中删除前100行，另存为新文件，然后：
```bash
python main.py --input "你的文件_批次2.xlsx" --output "./结果_批次2" --sample 100
```

---

### 3. 夜间运行

如果数据量特别大，可以晚上睡觉前运行：
```bash
python main.py --input "你的文件.xlsx" --output "./结果" --workers 3
```

程序会自动运行到完成，第二天早上查看结果即可。

---

## 📞 需要帮助？

如果遇到问题：
1. 查看命令提示符中的错误信息
2. 查看 `download_log.xlsx` 中的错误记录
3. 检查本文档的"常见问题"部分

---

## ✅ 完整操作清单

- [ ] 1. 安装Python（3.7+）
- [ ] 2. 安装依赖包（pip install ...）
- [ ] 3. 下载爬虫程序
- [ ] 4. 检查Excel文件表头是否为英文
- [ ] 5. 先用 `--sample 2` 测试
- [ ] 6. 测试成功后运行完整数据
- [ ] 7. 查看结果文件
- [ ] 8. 检查 `valid_announcements.xlsx` 中的有效公告

---

## 🎓 示例：完整操作流程

假设你的情况是：
- Excel文件：`C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx`
- 想保存到：`C:\Users\10657\Desktop\并购公告\爬取结果`

**完整步骤**：

```bash
# 1. 打开命令提示符，进入项目目录
cd D:\cninfo_crawler

# 2. 先测试2条数据
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\测试结果" --sample 2

# 3. 检查测试结果，如果正常，运行完整数据
python main.py --input "C:\Users\10657\Desktop\并购公告\并购公告下载.xlsx" --output "C:\Users\10657\Desktop\并购公告\爬取结果"

# 4. 等待完成，查看结果
```

---

## 🎉 成功标志

运行成功后，你应该看到：

```
✓ 处理完成!
```

以及在输出目录中看到：
- ✅ `announcements/` 文件夹（包含PDF文件）
- ✅ `download_log.xlsx`
- ✅ `valid_announcements.xlsx`（最重要！）
- ✅ `invalid_announcements.xlsx`

**重点查看 `valid_announcements.xlsx`**，这个文件包含所有符合要求的公告！

---

**祝你使用顺利！如有问题，请参考"常见问题"部分或联系技术支持。**
